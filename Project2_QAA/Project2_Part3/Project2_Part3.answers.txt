They are stranded, and I would use the reverse for any future analysis. After graphing the counts (albeit haphazardly) using plotting.py, creating the plots ccox.png and crh.png, there were clearly more reads in a vertical line than a horizontal one, indicating that reverse would be the reads to use. It is strand specific, as if it were not, the counts should be equal across a sample.